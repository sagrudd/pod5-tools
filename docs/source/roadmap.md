# Roadmap

The maintained roadmap lives at the repository root in `roadmap.md`.

For Read the Docs builds, include the root roadmap content in future work using
a documentation build step or a Sphinx include strategy that remains compatible
with local and hosted builds.
